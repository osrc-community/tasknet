:80 {
    handle_path /api/* {
        reverse_proxy localhost:8000
    }

    handle {
        root * /usr/share/caddy
        try_files {path} {path}/ /index.html
        file_server browse
    }
}
